using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SlotController : MonoBehaviour {

    #region Variables
    #region  Public Variables
    [Header("Sots Properties")]
    /// <summary>
    /// Says If Menu has slots SystemMenu Enabled 
    /// </summary>
    [Tooltip(" Says If Menu has slots SystemMenu Enabled ")]
    public bool isSlotsEnabled;
    /// <summary>
    /// Says If Slots System Menu handle with one Slot fale or many Slots(true)
    /// </summary>
    [Tooltip("Says If Slots System Menu handle with one Slot fale or many Slots(true)")]
    public bool useManySlots;
    #endregion
    #endregion
    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
